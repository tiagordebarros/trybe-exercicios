const primeiroNumero = 1;
const segundoNumero = 2;
const terceiroNumero = 3;

//100 / 2 = 50 (Resto: 0) => PAR

if (primeiroNumero % 2 === 0 || segundoNumero % 2 === 0 || terceiroNumero % 2 === 0) {
    console.log(true);
} else {
    console.log(false);
}
